version: '2'
services:
    app:
        container_name: "phpmentors.workflower.app"
        image: "phpmentors/php-app:php70"
        volumes:
            - ".:/var/app"
        environment:
            TERM: "xterm"
            TZ: "Asia/Tokyo"
            LANG: "ja_JP.UTF-8"
